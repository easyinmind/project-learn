import React from "react";
import { add } from "./tools/tool";
import "./app.less";

export default () => {
  add();
  // import('./tools/tool').then(res => {
  //   console.log(res)
  // })
  return <div>test</div>;

  var a = 1
};
